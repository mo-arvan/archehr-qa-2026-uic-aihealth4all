experiment_dirs:
  - "outputs/2026-selected"
  - "outputs/2026-test"

verbose: false
top_n: 10

# Hydra auto-creates the output directory with timestamped subdirectories
# Override on CLI: hydra.run.dir=outputs/log_analysis/
